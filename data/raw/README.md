# Raw Data

This folder points to the original dataset already present in the repository root.

- `ds_test_task_dataset/`: symlink to the provided raw CSV files (`users`, `sessions`, `transactions`, `events`).

Do not edit raw files in place.
